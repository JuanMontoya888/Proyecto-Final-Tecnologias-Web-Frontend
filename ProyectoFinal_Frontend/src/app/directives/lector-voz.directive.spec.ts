import { LectorVozDirective } from './lector-voz.directive';

describe('LectorVozDirective', () => {
  it('should create an instance', () => {
    const directive = new LectorVozDirective();
    expect(directive).toBeTruthy();
  });
});
